# virtualenv
.venv/
venv/

# Python
__pycache__/
*.pyc

# Secrets — never commit
.env
credentials.json
token.json
*-credentials.json
service-account*.json
hypercare-automation-*.json
# Any *.json file at the project root (service-account key downloads land here)
/*.json

# Per-client configs contain spreadsheet IDs (may be sensitive) — commit example only
config/clients/*.json
!config/clients/example.json

# Legacy flat client list
config/clients.json

# ATS config files contain public URLs (safe to commit), but exclude nothing here
# config/ats/ is intentionally NOT gitignored
